---
id: Q058
source: mianshiya
category: Agent 与框架
title: 在⼤模型应⽤中，通常如何实现⻓短期记忆机制？
generated: 2026-07-23T15:41:19.803225
---

# 在⼤模型应⽤中，通常如何实现⻓短期记忆机制？

> 来源: 面试鸭题库 | 分类: Agent 与框架

⼤模型本⾝没有内置的⻓期记忆，每次对话都是独⽴的。要实现⻓短期记忆，得靠外部⼿段和架构设计来补⾜。
短期记忆⼀般通过上下⽂窗⼝管理。模型能处理的 token 数有限，⽐如 32K，把最近的对话历史拼接进去就⾏。关键
是怎么裁剪，别让上下⽂爆炸。常⻅的做法是滑动窗⼝、摘要压缩，或者⽤向量检索挑出最相关的⼏句历史，这样既
能保留关键信息，⼜不会超出⻓度限制。
⻓期记忆就得依赖外部存储了。⽤户的历史⾏为、偏好、重要事件这些数据，存到数据库⾥，⽐如⽤ 向量数据库 存语
义特征。每次新请求进来，先根据当前输⼊去检索最相关的历史⽚段，再把这些“记忆”注⼊ prompt。像
LlamaIndex 或 LangChain 这类框架，就提供了现成的记忆管理模块，⽀持会话级的状态保持。
状态同步也得考虑。如果应⽤是分布式的，会话状态不能只存在本地，得⽤ Redis 这种中间件做共享存储，保证每次
请求都能拿到⼀致的历史。
说⽩了，模型只负责“想”，不负责“记”。所有记忆相关的脏活累活，都是外围系统在扛。